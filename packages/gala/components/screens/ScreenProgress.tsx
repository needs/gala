import { ScreenProgress } from "../../lib/store";

export default function ScreenProgress({ screen }: { screen: ScreenProgress }) {
  return (
    <div>
      <h1>ScreenProgress</h1>
    </div>
  );
}
